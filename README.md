# 农作物病害识别分类项目 (PlantVillage Disease Classification)

[![Python](https://img.shields.io/badge/Python-3.10+-blue.svg)](https://www.python.org/)
[![PyTorch](https://img.shields.io/badge/PyTorch-2.9+-red.svg)](https://pytorch.org/)
[![License](https://img.shields.io/badge/License-MIT-green.svg)](LICENSE)

基于深度学习的农作物病害自动识别系统，采用 MobileNetV2 + SE-Block 注意力机制，在 PlantVillage 数据集上实现高精度病害分类。

---

## 📋 项目概述

本项目旨在通过计算机视觉技术，自动识别农作物叶片的病害类型，帮助农业生产实现早期病害检测和精准防治。

### 核心特性

- 🎯 **高精度识别**: 基于迁移学习的 MobileNetV2 骨干网络
- ⚡ **轻量高效**: SE-Block 注意力机制，参数量小，推理速度快
- 📊 **38类病害**: 涵盖14种作物的健康状态和多种病害类型
- 🔍 **可解释性**: Grad-CAM 热力图可视化模型关注区域
- 📈 **完整评估**: 准确率、精确率、召回率、F1-score、混淆矩阵

### 数据集信息

- **数据集**: PlantVillage Dataset
- **样本总数**: 54,305 张高清彩色图像
- **作物种类**: 14种（苹果、葡萄、玉米、番茄、土豆、樱桃等）
- **类别数量**: 38 个（包含健康和病害状态）
- **数据划分**: 训练集 70% / 验证集 15% / 测试集 15%

---

## 🚀 快速开始

### 环境要求

- Python >= 3.10
- CUDA >= 11.8 (可选，用于 GPU 加速)
- 8GB+ RAM
- 3GB+ 磁盘空间（存储数据集）

### 安装依赖

```bash
# 克隆仓库
git clone https://github.com/Michael-sms/Computer-Vision-the-PlanetVillage-.git
cd Computer-Vision-the-PlanetVillage-

# 使用 uv（推荐）
uv sync

# 或使用 pip
pip install -r requirements.txt
```

### 下载数据集

```bash
# 运行数据下载脚本
python DataDownload.py
```

数据集将自动下载到 `datasets/plantvillage dataset/` 目录。

---

## 📂 项目结构

```
final-project/
├── data/                      # 数据处理模块
│   ├── split_dataset.py      # 数据集划分（70%-15%-15%）
│   ├── augmentation.py       # 数据增强策略
│   ├── data_loader.py        # PyTorch DataLoader
│   └── visualize_samples.py  # 样本可视化
│
├── models/                    # 模型定义
│   ├── mobilenetv2_se.py     # MobileNetV2 + SE-Block
│   └── attention.py          # SE 注意力模块
│
├── training/                  # 训练流程
│   ├── train.py              # 训练主循环
│   ├── validate.py           # 验证函数
│   ├── losses.py             # 损失函数
│   └── lr_scheduler.py       # 学习率调度
│
├── evaluation/                # 评估模块
│   ├── __init__.py           # 模块导出
│   ├── metrics.py            # 评估指标计算
│   ├── confusion_analysis.py # 混淆矩阵分析
│   ├── gradcam.py            # Grad-CAM 可视化
│   ├── visualization.py      # 训练曲线与指标可视化
│   └── test.py               # 测试集评估主脚本
│
├── utils/                     # 工具函数
│   ├── constants.py          # 项目常量定义
│   ├── helpers.py            # 辅助函数
│   └── device.py             # 设备管理
│
├── datasets/                  # 数据集目录
│   ├── plantvillage dataset/ # 原始数据集
│   └── splits/               # 划分索引文件
│
├── checkpoints/               # 模型权重保存
├── results/                   # 结果输出
│   ├── data_processing/      # 数据处理可视化结果
│   └── evaluated/            # 模型评估结果
├── docs/                      # 文档
│   ├── PROJECT_PLAN.md       # 项目计划
│   ├── DATA_USAGE.md         # 数据使用说明
|   └── EVALUATION_USAGE.md         # 评估使用说明
│
├── config.yaml                # 配置文件
├── main_train.py              # 训练入口
└── README.md                  # 本文件
```

---

## 🔧 使用方法

### 1. 数据准备

#### 划分数据集

```bash
python -m data.split_dataset
```

生成的划分文件保存在 `datasets/splits/`:
- `train.txt` - 37,997 张图像 (70%)
- `val.txt` - 8,129 张图像 (15%)
- `test.txt` - 8,179 张图像 (15%)

#### 生成可视化

```bash
python -m data.visualize_samples
```

可视化结果保存在 `results/data_processing/`:
- 类别样本展示
- 数据增强效果
- 类别分布图
- 划分分布图

### 2. 模型训练

```bash
# 使用默认配置训练
python main_train.py

# 或指定配置文件
python main_train.py --config config.yaml
```

**关键训练参数**（在 `config.yaml` 中配置）:
```yaml
model:
  backbone: mobilenet_v2
  num_classes: 38
  pretrained: true

training:
  batch_size: 32
  epochs: 50
  learning_rate: 0.0002
  weight_decay: 1e-5
  
data:
  train_ratio: 0.70
  val_ratio: 0.15
  test_ratio: 0.15
```

### 3. 模型评估

```bash
# 完整评估（包含 Grad-CAM 可视化）
python -m evaluation.test --checkpoint checkpoints/best_model.pth

# 快速评估（跳过 Grad-CAM）
python -m evaluation.test --no-gradcam

# 指定输出目录
python -m evaluation.test --output-dir results/evaluated
```

评估结果保存在 `results/evaluated/`:
- `metrics_report.json` - 完整评估指标
- `confusion_matrix_full.png` - 38类完整混淆矩阵
- `confusion_matrix_simplified.png` - 错误率最高类别混淆矩阵
- `confusion_analysis.txt` - 混淆分析报告
- `gradcam_summary.png` - Grad-CAM 汇总图
- `gradcam_samples/` - 各类别 Grad-CAM 样本

### 4. 生成训练曲线与指标可视化

```python
from evaluation.visualization import plot_training_history, plot_class_metrics

# 绘制训练曲线（Loss/Acc/LR）
plot_training_history()

# 绘制类别指标对比图（最佳/最差 F1 类别）
plot_class_metrics()
```

---

## 📊 技术方案

### 网络架构

```
输入图像 (224×224×3)
    ↓
[数据增强]
  - 随机旋转 (±20°)
  - 随机翻转
  - 颜色抖动
  - 高斯噪声
  - Cutout
    ↓
[MobileNetV2 骨干网络]
  - 18个瓶颈残差块
  - 深度可分离卷积
  - 参数量: 3.5M
    ↓
[SE-Block 注意力模块]
  - 通道级注意力
  - 压缩比: r=16
  - 插入位置: 最后4个MBConv块
    ↓
[分类头]
  - 全局平均池化
  - Dropout (p=0.2)
  - 全连接层 (1280 → 38)
    ↓
输出 (38类概率分布)
```

### 训练策略

- **迁移学习**: 使用 ImageNet 预训练权重
- **冻结策略**: 冻结前10层 (8.3%)，微调后续层
- **优化器**: Adam (lr=1e-4, weight_decay=1e-5)
- **学习率调度**: CosineAnnealingLR (T_max=100, eta_min=1e-6)
- **损失函数**: CrossEntropyLoss + Label Smoothing (ε=0.1)
- **早停**: 验证准确率连续10轮不提升则停止
- **梯度裁剪**: max_norm=1.0

### 数据增强

**训练集**:
- 随机旋转 (±20°)
- 随机翻转 (水平/垂直)
- 随机缩放裁剪 (0.8-1.0)
- 颜色抖动 (亮度/对比度/饱和度)
- 高斯噪声 (std=0.02, p=0.3)
- Cutout (size=16, p=0.2)

**验证/测试集**:
- 仅 Resize + 中心裁剪 + 标准化

---

## 📈 实验结果

### 训练信息

| 配置项 | 值 |
|--------|-----|
| GPU | NVIDIA GeForce RTX 4090 (24GB) |
| Batch Size | 128 |
| 训练轮数 | 27 (早停于第27轮) |
| 最佳验证准确率 | 99.48% (Epoch 17) |
| 总参数量 | 2.50M |
| 可训练参数 | 2.31M (92.6%) |

### 性能指标

| 指标 | 训练集 | 验证集 | 测试集 |
|------|--------|--------|--------|
| 准确率 (Accuracy) | 99.85% | 99.48% | **99.72%** |
| Top-3 准确率 | - | - | **99.98%** |
| Top-5 准确率 | - | ~100% | **99.99%** |
| 宏平均精确率 (Macro Precision) | - | - | 99.64% |
| 宏平均召回率 (Macro Recall) | - | - | 99.15% |
| 宏平均 F1 (Macro F1) | - | - | **99.37%** |
| 加权精确率 (Weighted Precision) | - | - | 99.72% |
| 加权召回率 (Weighted Recall) | - | - | 99.72% |
| 加权 F1 (Weighted F1) | - | - | **99.72%** |

### 测试集详细统计

- **总样本数**: 8,179
- **正确分类**: 8,156
- **错误分类**: 23
- **评估耗时**: 114.15 秒

### 最佳/最差类别性能

**准确率 100% 的类别 (共28个)**:
苹果-黑星病、苹果-黑腐病、苹果-雪松锈病、蓝莓-健康、樱桃-健康、樱桃-白粉病、玉米-锈病、玉米-健康、葡萄-黑腐病、葡萄-黑麻疹、葡萄-健康、葡萄-叶斑病、橙子-黄龙病、桃子-细菌斑点病、桃子-健康、甜椒-细菌斑点病、土豆-早疫病、土豆-晚疫病、树莓-健康、大豆-健康、南瓜-白粉病、草莓-健康、草莓-叶焦病、番茄-细菌斑点病、番茄-健康、番茄-晚疫病、番茄-黄化曲叶病毒

**准确率较低的类别**:
| 类别 | 准确率 | 正确/总数 |
|------|--------|-----------|
| 土豆-健康 | 83.33% | 20/24 |
| 玉米-叶枯病 | 95.97% | 143/149 |
| 玉米-灰斑病 | 96.15% | 75/78 |
| 番茄-花叶病毒 | 96.49% | 55/57 |
| 番茄-叶霉病 | 98.61% | 142/144 |

### 最易混淆的类别对

| 真实类别 | 预测类别 | 误分类率 | 数量 |
|----------|----------|----------|------|
| 土豆-健康 | 甜椒-健康 | 8.33% | 2 |
| 土豆-健康 | 大豆-健康 | 8.33% | 2 |
| 玉米-叶枯病 | 玉米-灰斑病 | 4.03% | 6 |
| 玉米-灰斑病 | 玉米-叶枯病 | 3.85% | 3 |

### 混淆矩阵

混淆矩阵和详细的类别级性能分析可在 `results/evaluated/` 目录查看：
- 完整 38×38 混淆矩阵热力图
- 错误率最高类别的简化混淆矩阵
- 最易混淆类别对分析报告

### Grad-CAM 可视化

模型通过 Grad-CAM 可视化显示，网络能够正确关注叶片上的病变区域，而非背景噪声。
可视化结果包括：
- 汇总图（16个样本的原图与热力图对比）
- 各类别样本的 Grad-CAM 热力图（每类3张）

---

## 🛠️ API 使用示例

### 加载数据

```python
from data import get_all_dataloaders

train_loader, val_loader, test_loader = get_all_dataloaders(
    batch_size=32,
    num_workers=4,
    use_weighted_sampler=True  # 处理类不平衡
)
```

### 创建模型

```python
from models import MobileNetV2WithSE
from utils.constants import NUM_CLASSES

model = MobileNetV2WithSE(
    num_classes=NUM_CLASSES,
    pretrained=True
)
```

### 训练循环

```python
from training import train_epoch, validate

for epoch in range(num_epochs):
    # 训练
    train_loss, train_acc = train_epoch(
        model, train_loader, criterion, optimizer, device, epoch
    )
    
    # 验证
    val_loss, val_acc = validate(
        model, val_loader, criterion, device
    )
    
    print(f"Epoch {epoch+1}: Train Acc={train_acc:.2f}%, Val Acc={val_acc:.2f}%")
```

详细使用说明请参考 [数据使用文档](docs/DATA_USAGE.md)。

---

## 📚 文档

- [项目计划](docs/PROJECT_PLAN.md) - 完整的项目计划和两人分工方案
- [数据使用说明](docs/DATA_USAGE.md) - 数据处理模块的详细使用文档
- [评估模块使用说明](docs/EVALUATION_USAGE.md) - 模型评估与可视化的详细使用文档

---

## 🔍 项目进度

- [x] 数据集下载和预处理
- [x] 数据集划分（70%-15%-15%）
- [x] 数据增强策略实现
- [x] DataLoader 和 Dataset 类
- [x] 样本可视化功能
- [x] MobileNetV2 + SE-Block 模型实现
- [x] 训练流程开发
- [x] 模型训练和调优
- [x] 评估指标计算（Accuracy/Precision/Recall/F1/Top-K）
- [x] 混淆矩阵分析与可视化
- [x] Grad-CAM 可解释性可视化
- [x] 训练曲线与类别指标可视化
- [ ] 完整测试和最终报告

---

## 👥 团队分工

### 第一人：模型与训练模块
- ✅ MobileNetV2 + SE-Block 模型实现
- ✅ 训练流程和验证逻辑
- ✅ 学习率调度和早停机制
- ✅ 超参数调优

### 第二人：数据处理与评估模块
- ✅ 数据集划分和加载
- ✅ 数据增强策略
- ✅ 样本可视化
- ✅ 评估指标计算（Accuracy/Precision/Recall/F1/Top-K）
- ✅ 混淆矩阵分析与可视化
- ✅ Grad-CAM 可解释性可视化
- ✅ 训练曲线与类别指标可视化

---

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

---

## 📄 许可证

本项目采用 MIT 许可证 - 详见 [LICENSE](LICENSE) 文件。

---

## 🙏 致谢

- **数据集**: [PlantVillage Dataset](https://www.kaggle.com/datasets/mohitsingh1804/plantvillage)
- **框架**: [PyTorch](https://pytorch.org/)
- **预训练模型**: [torchvision.models](https://pytorch.org/vision/stable/models.html)

---

## 📧 联系方式

如有问题，请通过 GitHub Issues 联系我们。

---

**最后更新**: 2026年1月26日
